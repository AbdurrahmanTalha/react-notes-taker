
import React from 'react';
import { Button } from 'react-bootstrap';

const Home = () => {
    return (
        <div>
            <Button type="primary">Home</Button>
        </div>
    );
};

export default Home;